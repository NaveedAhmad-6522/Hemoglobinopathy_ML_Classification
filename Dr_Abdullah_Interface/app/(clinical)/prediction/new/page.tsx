"use client";

import { useState } from "react";
import { useRouter } from "next/navigation";
import { ArrowRight, CircleAlert, ClipboardCheck, FlaskConical, RotateCcw } from "lucide-react";
import { PageHeading } from "@/components/page-elements";

type Values = Record<string, string>;
const fields = [
  ["hba", "Hb-A", "%", "89.4"], ["hba2", "Hb-A2", "%", "4.9"], ["hbf", "Hb-F", "%", "0.8"], ["hbs", "Hb-S", "%", "0.0"], ["hbd", "Hb-D", "%", "0.0"],
  ["rbc", "RBC", "×10¹²/L", "5.21"], ["hgb", "HGB", "g/dL", "10.8"], ["hct", "HCT", "%", "35.5"], ["mcv", "MCV", "fL", "68.2"], ["mch", "MCH", "pg", "20.7"], ["mchc", "MCHC", "g/dL", "30.4"],
] as const;
const initial = Object.fromEntries(fields.map(([key,, ,value]) => [key, value])) as Values;

export default function NewPredictionPage() {
  const router = useRouter(); const [values, setValues] = useState<Values>(initial); const [age, setAge] = useState("24"); const [gender, setGender] = useState("Female"); const [submitted, setSubmitted] = useState(false);
  const valid = age !== "" && Object.values(values).every((value) => value !== "" && Number.isFinite(Number(value)) && Number(value) >= 0);
  const submit = (e: React.FormEvent) => { e.preventDefault(); setSubmitted(true); if (!valid) return; const query = new URLSearchParams({ ...values, age, gender }); router.push(`/results?${query.toString()}`); };
  return <div className="mx-auto max-w-5xl"><PageHeading eyebrow="Clinical assessment" title="New hemoglobinopathy prediction" description="Enter the patient’s hematological profile to generate an AI-assisted classification. Values are evaluated using the XGBoost model workflow." /><form onSubmit={submit}><div className="grid gap-5 lg:grid-cols-[1fr_280px]"><div className="space-y-5"><section className="surface p-5 sm:p-6"><div className="flex items-start gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-brand-50 text-brand-600 dark:bg-brand-900/40 dark:text-sky-400"><ClipboardCheck size={20} /></span><div><h2 className="font-bold text-slate-900 dark:text-white">Patient details</h2><p className="mt-1 text-xs leading-5 text-slate-500">Patient identifiers are excluded in this prototype.</p></div></div><div className="mt-5 grid gap-4 sm:grid-cols-2"><label className="text-sm font-semibold text-slate-700 dark:text-slate-200">Age <span className="text-rose-500">*</span><input type="number" min="0" max="120" value={age} onChange={(e) => setAge(e.target.value)} className="input" placeholder="e.g. 24" /></label><label className="text-sm font-semibold text-slate-700 dark:text-slate-200">Gender <span className="text-rose-500">*</span><select value={gender} onChange={(e) => setGender(e.target.value)} className="input"><option>Female</option><option>Male</option></select></label></div>{submitted && !valid && <p className="mt-4 flex items-center gap-2 text-xs font-medium text-rose-600"><CircleAlert size={15} />Please complete age and all numeric test values with valid, non-negative numbers.</p>}</section><section className="surface p-5 sm:p-6"><div className="flex items-start gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-violet-50 text-violet-600 dark:bg-violet-900/30 dark:text-violet-400"><FlaskConical size={20} /></span><div><h2 className="font-bold text-slate-900 dark:text-white">Laboratory values</h2><p className="mt-1 text-xs leading-5 text-slate-500">Enter hemoglobin fractions and complete blood count values.</p></div></div><div className="mt-6"><p className="mb-3 text-xs font-bold uppercase tracking-[.14em] text-slate-400">Hemoglobin fractions</p><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{fields.slice(0,5).map(([key,label,unit,placeholder]) => <Field key={key} fieldKey={key} label={label} unit={unit} placeholder={placeholder} values={values} setValues={setValues} />)}</div><p className="mb-3 mt-7 text-xs font-bold uppercase tracking-[.14em] text-slate-400">Complete blood count</p><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{fields.slice(5).map(([key,label,unit,placeholder]) => <Field key={key} fieldKey={key} label={label} unit={unit} placeholder={placeholder} values={values} setValues={setValues} />)}</div></div></section></div><aside className="space-y-4"><div className="surface p-5"><p className="eyebrow">Assessment setup</p><h2 className="mt-2 font-bold text-slate-900 dark:text-white">Ready to classify</h2><p className="mt-2 text-xs leading-5 text-slate-500">The result will include class probabilities and a clinical interpretation.</p><div className="mt-5 rounded-xl bg-slate-50 p-3 dark:bg-slate-950"><div className="flex justify-between text-xs"><span className="text-slate-500">Model</span><span className="font-bold text-slate-700 dark:text-white">XGBoost v2.4</span></div><div className="mt-2 flex justify-between text-xs"><span className="text-slate-500">Classes</span><span className="font-bold text-slate-700 dark:text-white">06</span></div></div><button className="btn-primary mt-5 w-full py-3" type="submit">Generate prediction <ArrowRight size={17} /></button><button type="button" onClick={() => { setValues(initial); setAge("24"); setGender("Female"); setSubmitted(false); }} className="btn-secondary mt-2 w-full"><RotateCcw size={15} />Reset form</button></div><div className="rounded-2xl border border-sky-100 bg-sky-50 p-4 text-xs leading-5 text-slate-600 dark:border-sky-900/50 dark:bg-sky-950/30 dark:text-slate-300"><p className="font-bold text-brand-700 dark:text-sky-300">Clinical support notice</p><p className="mt-1">This prototype is an assistive tool. Final interpretation requires clinical correlation and confirmatory testing.</p></div></aside></div></form></div>;
}
function Field({ fieldKey, label, unit, placeholder, values, setValues }: { fieldKey: string; label: string; unit: string; placeholder: string; values: Values; setValues: React.Dispatch<React.SetStateAction<Values>> }) { return <label className="text-sm font-semibold text-slate-700 dark:text-slate-200">{label}<div className="relative"><input type="number" min="0" step="any" value={values[fieldKey]} onChange={(e) => setValues((v) => ({ ...v, [fieldKey]: e.target.value }))} className="input pr-16" placeholder={placeholder} /><span className="absolute right-3 top-3.5 text-[10px] font-bold text-slate-400">{unit}</span></div></label>; }
